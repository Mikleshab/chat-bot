query GetMessagesAnalytics($input: GetAnalyticsInput!) {
    getMessagesAnalytics(input: $input) {
        title
        count
        diff
        icon
        history {
            title
            color
            text
            value
        }
    }
}
