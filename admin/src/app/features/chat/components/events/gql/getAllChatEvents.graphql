query GetAllChatEvents($input: GetAllChatEventInput!) {
    getAllChatEvents(input: $input) {
        id
        chatId
        title
        eventOptions {
            type
            system
            frequencyType
            interval
            startDate
            endDate
        }
    }
}
