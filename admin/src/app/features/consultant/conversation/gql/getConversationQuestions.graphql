query GetConversationQuestions($input: GetConversationInput!) {
    getConversation(input: $input) {
        messages {
            id
            content
            timestamp
            parentId
            replyCount
            author {
                userId
                username
                fullName
            }
        }
    }
}
