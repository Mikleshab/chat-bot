subscription OnMessageUpdated {
    MessageUpdatedEvent {
        id
        content
        timestamp
        parentId
        replyCount
        author {
            userId
            username
            fullName
        }
    }
}
