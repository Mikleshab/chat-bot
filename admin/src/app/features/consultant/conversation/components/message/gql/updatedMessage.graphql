subscription OnMessageUpdated($input: GetRepliesInput!) {
    MessageUpdatedEvent(input: $input) {
        id
        content
        timestamp
        parentId
        replyCount
        author {
            userId
            username
            fullName
        }
    }
}
